{
  "name": "vic-tour",
  "version": "1.0.0",
  "description": "VIC Tour - Sports Tournament Intelligence Engine",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "jest --coverage",
    "init-data": "node scripts/init-tournaments.js",
    "init-championships": "node scripts/init-championships.js",
    "postinstall": "npm run init-data && npm run init-championships"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "node-cron": "^3.0.3",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
